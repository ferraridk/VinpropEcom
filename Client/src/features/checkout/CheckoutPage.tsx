import { Typography } from "@mui/material";

export default function CheckoutPage() {
  return (
    <Typography variant="h3">
      Kun brugere som er logget ind burde kunne se denne side!
    </Typography>
  );
}
